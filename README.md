Reload Window
sudo chmod 666 /var/run/docker.sock

For login: You can immediately issue the user a token (JWT), since you have all their data on hand.

git revert плюс хеш

git reset если не отправлен коммит на гит.

Jeg fyller opp mer in Models

dotnet ef migrations remove
dotnet ef database drop --force

dotnet ef migrations add AddRbacTables
dotnet ef database update
